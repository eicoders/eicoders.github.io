<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jar Corporation | For You</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <style>
        :root {
            --bg-color: #050505;
            --surface: rgba(255, 255, 255, 0.03);
            --border: rgba(255, 255, 255, 0.08);
            --text-primary: #FFFFFF;
            --text-secondary: rgba(255, 255, 255, 0.5);
            --wa-green: #25D366;
            --accent-blue: #007AFF; /* Apple/Google Blue */
            --transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; -webkit-font-smoothing: antialiased; }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow: hidden;
        }

        /* Ambient Glow in Background */
        body::before {
            content: "";
            position: absolute;
            width: 300px; height: 300px;
            background: radial-gradient(circle, rgba(0, 122, 255, 0.15) 0%, transparent 70%);
            top: -100px; right: -100px; z-index: -1;
        }

        .main-card {
            width: 100%;
            max-width: 420px;
            background: var(--surface);
            backdrop-filter: blur(40px);
            -webkit-backdrop-filter: blur(40px);
            border: 1px solid var(--border);
            border-radius: 32px;
            padding: 48px 32px;
            text-align: center;
            box-shadow: 0 40px 80px rgba(0, 0, 0, 0.4);
            animation: cardEntrance 1s var(--transition);
        }

        @keyframes cardEntrance {
            from { opacity: 0; transform: translateY(30px) scale(0.98); }
            to { opacity: 1; transform: translateY(0) scale(1); }
        }

        /* Brand Identity */
        .logo-container { margin-bottom: 32px; }
        .logo {
            width: 84px; height: 84px;
            filter: drop-shadow(0 0 20px rgba(255,255,255,0.1));
            transition: var(--transition);
        }
        .logo:hover { transform: scale(1.05) rotate(5deg); }

        h1 {
            font-size: 1.8rem; font-weight: 800;
            letter-spacing: -0.8px; margin-bottom: 12px;
            background: linear-gradient(180deg, #FFFFFF 0%, rgba(255,255,255,0.7) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
        }

        .sub-headline {
            font-size: 0.95rem; color: var(--text-secondary);
            line-height: 1.6; margin-bottom: 40px; font-weight: 400;
        }

        /* Action Buttons */
        .primary-cta {
            background: #FFFFFF;
            color: #000000;
            width: 100%;
            padding: 18px;
            border-radius: 18px;
            text-decoration: none;
            font-weight: 700;
            font-size: 1rem;
            display: block;
            margin-bottom: 20px;
            transition: var(--transition);
        }

        .primary-cta:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(255,255,255,0.15);
            background: #f0f0f0;
        }

        /* WhatsApp Premium Component */
        .wa-box {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 16px;
            text-decoration: none;
            transition: var(--transition);
        }

        .wa-box:hover {
            background: rgba(37, 211, 102, 0.08);
            border-color: var(--wa-green);
            transform: translateY(-2px);
        }

        .wa-left { display: flex; align-items: center; gap: 14px; }
        
        .wa-icon-frame {
            width: 44px; height: 44px;
            background: rgba(37, 211, 102, 0.1);
            border-radius: 12px;
            display: flex; align-items: center; justify-content: center;
            color: var(--wa-green); font-size: 22px;
        }

        .wa-info { text-align: left; }
        .wa-info b { display: block; font-size: 0.95rem; color: #fff; }
        .wa-info span { font-size: 0.75rem; color: var(--text-secondary); }

        .join-badge {
            background: var(--wa-green);
            color: #000000;
            padding: 8px 14px;
            border-radius: 10px;
            font-size: 0.75rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Refined Progress Bar */
        .loading-wrap { margin-top: 48px; position: relative; }
        
        .progress-track {
            width: 100%; height: 3px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px; overflow: hidden;
            margin-bottom: 12px;
        }

        .progress-bar {
            height: 100%; width: 0%;
            background: linear-gradient(90deg, var(--accent-blue), #FFFFFF);
            border-radius: 10px;
            animation: grow 10s linear forwards;
        }

        @keyframes grow { from { width: 0%; } to { width: 100%; } }

        .status-msg {
            font-size: 0.7rem; font-weight: 600;
            color: var(--text-secondary);
            text-transform: uppercase; letter-spacing: 2px;
        }

        #timer { color: #fff; }

        /* Mobile Optimization */
        @media (max-width: 480px) {
            .main-card { padding: 40px 24px; }
            h1 { font-size: 1.5rem; }
        }
    </style>
</head>
<body>

    <div class="main-card">
        <div class="logo-container">
            <img src="my_logo.png" alt="Jar Co. Brand" class="logo">
        </div>
        
        <h1>RRC Alwar Portal</h1>
        <p class="sub-headline">Jar Corporation is securely routing your connection to the official student portal.</p>
        
        <a href="https://rrcalwar.onlineexamforms.com/student.aspx" class="primary-cta">
            <i class="fa-solid fa-arrow-right-to-bracket" style="margin-right: 8px;"></i> OPEN PORTAL
        </a>

        <a href="https://whatsapp.com/channel/0029Vb5gZjY6GcGG7Tllv218" class="wa-box" target="_blank">
            <div class="wa-left">
                <div class="wa-icon-frame">
                    <i class="fab fa-whatsapp"></i>
                </div>
                <div class="wa-info">
                    <b>Jar Corporation</b>
                    <span>Official Fanmade Channel</span>
                </div>
            </div>
            <div class="join-badge">Join</div>
        </a>

        <div class="loading-wrap">
            <div class="progress-track">
                <div class="progress-bar"></div>
            </div>
            <p class="status-msg">Redirecting in <span id="timer">10</span>s</p>
        </div>
    </div>

    <script>
        // High-Precision Timer Logic
        let timeLeft = 10;
        const timerDisplay = document.getElementById('timer');
        const targetURL = "https://rrcalwar.onlineexamforms.com/student.aspx";

        const countdown = setInterval(() => {
            timeLeft--;
            timerDisplay.innerText = timeLeft;
            
            if (timeLeft <= 0) {
                clearInterval(countdown);
                window.location.replace(targetURL); // replace avoids back-button loops
            }
        }, 1000);
    </script>

</body>
</html>

